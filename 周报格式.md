# 周报格式

## 总体要求
1. 周报文件用于个人填报和后续部门汇总，格式必须固定，便于 skill 自动检查、归类和合并。
2. 周报按固定板块填写，板块顺序不得调整：
   金融研发、寄递研发、管理赋能、数据分析、信息资源管理、队伍建设及业绩融合、下周计划。
3. 每条记录只描述一项相对完整的工作，不要把多个事项混写在同一行。
4. 周报涉及的系统名称、机构名称、部门名称，统一参照 [标准名称.md](./标准名称.md)。

## 文件名要求

文件名固定为：`人名工作周报YYYYMMDD.md`

说明：
- `YYYYMMDD` 使用本周期结束日期，不使用实际提交日期。
- 示例：`郭思文工作周报20260326.md`

## 文件头要求

周报文件头使用固定 YAML front matter：

```yaml
---
姓名: 郭思文
填报周期开始: 2026-03-20
填报周期结束: 2026-03-26
---
```

说明：
- 文件头必须位于文件开头。
- 日期统一使用 `YYYY-MM-DD` 格式，便于程序校验和排序。
- 周报正文中不再重复填写姓名和填报周期。

## 周报正文格式

每个板块使用二级标题和标准 markdown 表格。

固定字段如下：
- `项目名称`：必须使用标准名称；没有项目时使用标准事项名称。
- `开始时间`：使用 `YYYY-MM-DD` 格式。
- `结束时间`：使用 `YYYY-MM-DD` 格式；截至填报日仍未完成时留空。
- `当前状态`：建议使用“已完成、进行中、已提交待确认、已上线、已提测、待启动”等标准表达。
- `工作内容`：描述本周实际开展的动作。
- `工作成果`：描述本周已形成的结果、产出或结论；没有明确结果时可留空。

标准表头如下：

```md
| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 | 工作成果 |
| --- | --- | --- | --- | --- | --- |
```

`下周计划` 板块使用单独表头：

```md
| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 |
| --- | --- | --- | --- | --- |
```

## 空板块规则

1. 即使某个板块本周没有内容，也必须保留板块标题。
2. 没有内容时，不写空表格，在标题下单独写一行 `无`。
3. `下周计划` 不能写 `无`，如确实没有明确计划，需人工确认后再提交。

## 填写规则

1. 同一项目下如果是不同目标或不同结果的工作，必须拆成多行填写。
2. 同一事项连续多天推进、目标一致且结果一致时，可以合并成一行。
3. `工作内容` 侧重过程，`工作成果` 侧重结果，二者不要重复。
4. 一行记录中只保留一个主项目名称，不在项目名称中混入部门名称、人员姓名或多个系统名。
5. 如果事项跨多个板块，不允许一行多分类，必须拆分到各自板块。
6. 表格内不要使用模糊表述，例如“做了一些优化”“处理相关问题”“持续推进若干工作”。

## 工作分类判定规则

### 1. 下周计划
- 只要是下个周期计划开展的事项，统一填写到 `下周计划`。
- `下周计划` 的开始时间必须晚于本周期结束日期。

### 2. 信息资源管理
- 涉及省内云平台、超融合平台、集团云平台、数据中台、数据下载平台的运行运维、环境维护、权限处理、系统迁移、数据提取等事项，归入 `信息资源管理`。
- 如果“数据提取”本质上是平台运行支撑、报表导出、底表提供，也归入本板块。

### 3. 数据分析
- 以专题分析、指标分析、经营分析、数据核查、对标分析为主要输出的事项，归入 `数据分析`。
- 如果工作核心产出是分析结论、分析报告、分析口径或分析结果，而不是系统开发或平台运维，优先归入本板块。

### 4. 金融研发、寄递研发、管理赋能
- 这三个板块仅填写项目研发或项目建设类事项。
- 优先根据项目服务的主要需求部门判断：
  - 面向金融条线的项目，归入 `金融研发`。
  - 面向寄递、运管、速递物流等条线的项目，归入 `寄递研发`。
  - 不属于以上两类的其他研发、工具开发、支撑系统建设，归入 `管理赋能`。

### 5. 队伍建设及业绩融合
- 涉及需求对接、方案研讨、技术交流、技术支持、材料编写、软件著作权、数据上报、会议支撑、工作时间内开展的培训等事项，归入 `队伍建设及业绩融合`。
- 如果事项不是项目研发交付本身，而是沟通、协调、汇报、支撑、培训、材料类工作，优先归入本板块。

### 6. 冲突处理优先级

当一条工作可能同时符合多个板块时，按以下顺序判定：

1. 是否属于 `下周计划`
2. 是否属于 `信息资源管理`
3. 是否属于 `数据分析`
4. 是否属于项目研发类事项
5. 其余事务类事项归入 `队伍建设及业绩融合`

## 项目名称规则

1. `金融研发`、`寄递研发`、`管理赋能`、`数据分析` 板块原则上按项目名称填写。
2. `信息资源管理`、`队伍建设及业绩融合` 板块原则上按标准事项名称填写，例如：`系统运维`、`数据提取`、`培训开展`、`需求研讨`、`材料编写`。
3. 同一项目组内，不同人员在同一周期和不同周期中，必须使用同一标准名称。
4. 机构和部门名称必须使用标准名称，例如：`省寄递业务部`，不能写成 `省寄递部`；`版纳州分公司`，不能写成 `版纳公司`。

## 排序规则

1. 各板块按照固定顺序出现。
2. 同一板块内先按 `项目名称` 排序，再按 `开始时间` 升序排列。
3. `下周计划` 同样按照 `项目名称` 和 `开始时间` 排序。

## 周报内容格式示例

```md
---
姓名: 郭思文
填报周期开始: 2026-03-20
填报周期结束: 2026-03-26
---

## 金融研发

| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 | 工作成果 |
| --- | --- | --- | --- | --- | --- |
| 金融员工履职系统 | 2026-03-20 | 2026-03-25 | 已完成 | 优化员工履职上传地理信息位置偏差功能 | 完成位置偏差处理逻辑优化并提交测试 |

## 寄递研发

| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 | 工作成果 |
| --- | --- | --- | --- | --- | --- |
| 车牌寄递辅助系统 | 2026-03-20 | 2026-03-25 | 进行中 | 持续优化顺丰和京东订单接口 | 完成接口联调问题排查并修复部分报文处理逻辑 |

## 管理赋能

| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 | 工作成果 |
| --- | --- | --- | --- | --- | --- |
| 文创产品进销存系统 | 2026-03-23 | 2026-03-25 | 已完成 | 开发销售小票打印功能 | 完成销售小票打印功能开发并交付测试 |

## 数据分析

| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 | 工作成果 |
| --- | --- | --- | --- | --- | --- |
| 抖音指数分析 | 2026-03-23 | 2026-03-23 | 已完成 | 开展省寄递业务部抖音指数数据核实 | 输出核实结果并反馈异常数据明细 |

## 信息资源管理

| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 | 工作成果 |
| --- | --- | --- | --- | --- | --- |
| 系统运维 | 2026-03-23 | 2026-03-25 | 已完成 | 开展省内云平台操作系统底层加固工作 | 完成目标主机加固处理 |
| 数据提取 | 2026-03-23 | 2026-03-23 | 已完成 | 提取省寄递业务部第三方对账数据 | 输出并提供第三方对账数据文件 |

## 队伍建设及业绩融合

| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 | 工作成果 |
| --- | --- | --- | --- | --- | --- |
| 培训开展 | 2026-03-20 | 2026-03-20 | 已完成 | 在邮务部全省培训推广会议上开展文创产品进销存系统使用培训 | 完成培训讲解和现场答疑 |
| 需求研讨 | 2026-03-24 | 2026-03-24 | 已完成 | 配合省运管部与重庆联通下属企业沟通无人包裹柜系统实施方案 | 形成实施方案沟通意见 |
| 材料编写 | 2026-03-23 | 2026-03-23 | 已完成 | 收集整理 2026 年拟申报软件著作权的项目材料 | 完成 7 个项目申报材料整理 |

## 下周计划

| 项目名称 | 开始时间 | 结束时间 | 当前状态 | 工作内容 |
| --- | --- | --- | --- | --- |
| 车牌寄递辅助系统 | 2026-03-27 | 2026-03-31 | 待启动 | 开发云邮便民查询服务对接接口 |
| 材料编写 | 2026-03-27 | 2026-03-28 | 待启动 | 编写工程施工监控系统申报集团创新项目所需实施方案和相关材料 |
```
