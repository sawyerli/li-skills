# TODO

用于记录这个仓库后续要实现的能力、规则优化和流程改进。

状态约定：
- `todo`：已确认要做
- `doing`：正在处理
- `done`：已经完成
- `idea`：先记录想法，暂未排期

优先级约定：
- `P1`：优先处理，影响当前主流程
- `P2`：重要但不阻塞当前使用
- `P3`：优化项或远期规划

## Now

- [x] P1 `merge-reports`：下周工作按业务板块处理
  状态：done
  说明：个人周报和部门周报已统一调整为“本周工作 / 下周工作”两大模块，`下周工作` 已按金融研发、寄递研发、管理赋能、数据分析、信息资源管理、队伍建设及业绩融合等业务板块分别归并和编写。

- [ ] P1 `流程`：目录结构的初始化操作
  状态：todo
  说明：为 `monthYYYYMM/weekYYYYMMDD/raw|apv|dept` 增加初始化流程，避免手工建目录。

- [ ] P1 `流程`：Excel 生成功能
  状态：todo
  说明：支持根据个人周报校验结果或部门周报汇总结果生成 Excel 文件，便于后续分发、留档和人工复核。

## Next

- [ ] P2 `validate-report`：自动生成校验结果摘要文件
  状态：todo
  说明：在周目录下输出校验汇总，便于人工复核 raw 和 apv 的差异。

- [ ] P2 `merge-reports`：统一部门周报输出文件命名与覆盖策略
  状态：todo
  说明：明确 `dept/` 下的文件名规则，以及重复生成时是覆盖、保留历史版本还是追加。

- [ ] P2 `merge-reports`：继续优化不同场景下的句式稳定性
  状态：todo
  说明：在已有场景化句式规则基础上，继续减少重复表达，增强正式汇报口径的一致性。

## Later

- [ ] P3 `validate-report`：标准名称统一化能力
  状态：todo
  说明：评估是否在校验阶段引入项目名称、部门名称、机构名称的标准化检查或修正建议。

- [ ] P3 `merge-reports`：更强的同项目归并能力
  状态：todo
  说明：增强多人同项目、多种描述方式下的归纳能力，减少人工润色需求。

## Ideas

- [ ] `流程`：生成月度汇总
  状态：idea
  说明：基于同一 `monthYYYYMM/` 下多个周目录，自动生成月度汇总材料。

- [ ] `流程`：将校验和汇总串成一条完整工作流
  状态：idea
  说明：支持从原始周报目录一键完成校验、归档、汇总和部门周报输出。
